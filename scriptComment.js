/* the same thing to flip*/
// function flipCard() {
//     if(this.classList.contains('flip')){                 
//         this.classList.remove('flip');
//     }else{
//         this.classList.add('flip');
//     }

//     if(this.classList.contains('rotate')){
//         this.classList.remove('rotate');
//     }else{
//         this.classList.add('rotate');
//     }
// }
/* the same thing to flip*/

/**************************************************************************************************** */

// (function checkforMatch () {
//     /* The previousCard variable is first set to null because when the game starts,
//      no card has been clicked yet, so there is no previously clicked card. */
//     // after it will store the src of the previously clicked card
// let previousCard = null;
//     // Add event listener to each card
// memoryCards.forEach(function(card) {
//   card.addEventListener("click", function() {
//     // Get the src of the image behind the card
//     let currentCard = card.querySelector("img").src;
//     // Compare current card to previous card
//     if (previousCard !== null) {
//       if (currentCard === previousCard) {
//         // Images are the same
//         console.log("Images are the same");
//       } else {
//         // Images are different
//         console.log("Images are different");
//       }
//     }
//     // Store the src of the previously clicked card)
//     previousCard = currentCard;
//   });
// });
// } ())
